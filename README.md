# BIP-work-scrapper
A local Python app that scrapes job offers from Polish BIP portals (bip.gov.pl), stores them in an SQLite database, and provides a filterable, user-friendly Streamlit interface for viewing and tracking active listings.
